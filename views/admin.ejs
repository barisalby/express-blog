<!DOCTYPE html>
<html lang="en">
  <head>
    <%-include('inc/head')%>
  </head>

  <body>
    <header><%-include('inc/Navbar')%></header>

    <main>
      <section>
        <div class="container">
          <div class="row">
            <a class="mb-3" href="admin/add">New Post</a>
            <% blogs.forEach(blog => { %>
            <h5><%=blog.title %></h5>
            <a href="#"><h5>Delete</h5></a>
          </div>
          <% }) %>
        </div>
      </section>
    </main>

    <footer></footer>

    <%-include('inc/Scripts')%>
  </body>
</html>
